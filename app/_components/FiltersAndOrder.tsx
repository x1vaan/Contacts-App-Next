import { CustomSelect } from "./CustomSelect";
export default async function FiltersAndOrder() {
  return (
    <section
      id="filters-order"
      className="w-[95%] h-[50px] mt-4 flex justify-center items-center bg-bgColor rounded-md relative"
    >
      {/* <CustomSelect /> */} Se sobrepone sobre los demas componentes, tengo que arreglarlos
    </section>
  );
}
